
name: Test
on: push
jobs:
  run:
    container: kipparker/docker-tape-run
    runs-on: ubuntu-latest
    steps:
    - run: ls
    - run: rm -rf node_modules
    - run: npm install browser-run
    - run: echo 'console.log(9001)' | ./node_modules/.bin/browser-run
