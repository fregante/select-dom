name: Test
on: push
jobs:
  run:
    runs-on: ubuntu-latest
    services:
      xvfb:
        image: metal3d/xvfb
    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
    - run: npm install
    - run: npm test
      env:
        DISPLAY: 'xvfb:99'
